[project]
name = "stopwatch-tutorial"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "textual>=8.2.4",
]

[dependency-groups]
dev = [
    "ruff>=0.15.11",
    "textual-dev>=1.8.0",
    "ty>=0.0.32",
]
